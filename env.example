# Environment Variables Template
# 
# IMPORTANT: This file is a template. Copy it to .env and fill in your actual values.
# The .env file is gitignored and will NOT be committed to version control.
#
# For Flutter apps, you can use environment variables in two ways:
#
# METHOD 1: Using --dart-define (Recommended for CI/CD)
#   flutter run --dart-define=SUPABASE_URL=xxx --dart-define=SUPABASE_ANON_KEY=yyy
#   flutter build apk --dart-define=SUPABASE_URL=xxx --dart-define=SUPABASE_ANON_KEY=yyy
#
# METHOD 2: Using flutter_dotenv (for local development)
#   Install: flutter pub add flutter_dotenv
#   Load in main.dart: await dotenv.load(fileName: ".env");
#   Access: dotenv.env['SUPABASE_URL']
#
# Supabase Configuration (REQUIRED)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Stripe Configuration (Optional - if using Stripe)
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Demo/Development Settings (Optional)
DEMO_MODE=true
DEBUG_MODE=true
